# Legacy

This folder is reserved for historical material that has not yet been migrated
into the current project structure.

The active runtime code is in `src/cable_driven_eye_robot/` and the runnable
demo entry points are in `demos/`. Earlier joystick and eye-tracker replay
scripts were replaced by modular demos and are no longer kept here.
