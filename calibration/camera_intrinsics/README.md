# Camera Intrinsics

Camera intrinsic calibration files for the ESP32-S3-CAM OV3660 modules.

Current files:

- `camera_parameter_left_or_default.npz`
- `camera_parameter_right.npz`

Each calibration artifact should record the camera identity, image resolution,
calibration target, collection date, and the script or notebook used to produce
it.
